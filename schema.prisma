datasource db {
  provider = "mysql"
  url      = "mysql://4o0lfwecdzr8eg449j2d:pscale_pw_ab7kO806KnrbX6rQ7OOdEpBHmdxinOkceDr52JfPUdh@aws.connect.psdb.cloud/berich?sslaccept=strict'"
  //relationMode = "prisma"
}

generator client {
  provider             = "prisma-client-py"
  previewFeatures      = ["fullTextSearch"]
  interface            = "asyncio"
  recursive_type_depth = "5"
}

model User {
  id        Int      @id @default(autoincrement())
  phone     String?  @default("")
  email     String?  @unique
  name      String
  password  String?
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  admin     Boolean  @default(false)
  Fav       Fav[]
}

model Item {
  id                  Int              @id @default(autoincrement())
  createdAt           DateTime         @default(now())
  updatedAt           DateTime         @updatedAt
  caseNumber          String
  caseIndex           String
  itemNumber          String
  itemType            String
  initialPrice        String
  minPrice            String
  bidType             String
  saleDate            DateTime
  description         String           @db.MediumText
  itemLocation        String
  court               String
  caseApplyDate       DateTime
  auctionApplyDate    DateTime
  allocationApplyDate DateTime?
  requestPrice        String
  detailOfList        String           @default("") @db.MediumText
  areaOfBuilding      Float
  areaOfGround        Float
  appraisal           String           @db.MediumText
  status              Int              @default(0)
  lastestStatus       String           @default("")
  numOfPass           Int              @default(0)
  isShareBuilding     Boolean          @default(false)
  isShareGround       Boolean          @default(false)
  resultAuction       resultAuction[]
  leaseDetail         leaseDetail[]
  leasePeople         leasePeople[]
  Fav                 Fav[]
  realestateList      realestateList[]
  images              String           @default("")
  uniqueCode          String           @default("")
  pdfHex              String           @default("") @db.LongText
  visited             Int              @default(0)
  copyCaseInfo        String           @default("") @db.LongText
  copyDateInfo        String           @default("") @db.LongText
  copyMailInfo        String           @default("") @db.LongText
  certification       certification[]
  Recommend           recommend[]
  adminComment        adminComment[]
}

model certification {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  rank      String
  purpose   String   @default("")
  date      DateTime
  detail    String   @db.MediumText
  creditor  String   @default("")
  amount    String   @default("")
  type      String   @default("")
  title     String   @default("")
  item      Item     @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId    Int

  @@index([itemId])
}

model recommend {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  item      Item     @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId    Int

  @@index([itemId])
}

model court {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  name      String
  code      String
}

model Image {
  id              Int      @id @default(autoincrement())
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  caseNumber      String
  cloudflareImgId String
  imgSrc          String   @default("")
}

model valuation {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  caseNumber String
  src        String   @default("")
}

model statementOfSale {
  id         Int      @id @default(autoincrement())
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  caseNumber String
  itemNumber Int
  src        String   @default("")
}

model resultAuction {
  id           Int       @id @default(autoincrement())
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
  date         DateTime?
  type         String?
  location     String?
  minSalePrice String?
  result       String?
  round        Int?      @default(0)
  item         Item      @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId       Int

  @@index([itemId])
}

model realestateList {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  num       Int
  type      String
  usage     String   @default("")
  location  String   @default("")
  detail    String?  @db.MediumText
  item      Item     @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId    Int

  @@index([itemId])
}

model leaseDetail {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  content   String?  @db.MediumText
  item      Item     @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId    Int

  @@index([itemId])
}

model leasePeople {
  id           Int      @id @default(autoincrement())
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
  leaseName    String?
  leaseType    String?
  deposit      String?
  transferDate String?
  confirmDate  String?
  occupancy    String?  @default("")
  period       String?  @default("")
  rent         String?  @default("")
  item         Item     @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId       Int

  @@index([itemId])
}

model Fav {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  userId    Int
  item      Item     @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId    Int

  @@index([itemId])
  @@index([userId])
}

model adminComment {
  id        Int      @id @default(autoincrement())
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  category  String
  title     String
  comment   String   @db.MediumText
  item      Item     @relation(fields: [itemId], references: [id], onDelete: Cascade)
  itemId    Int

  @@index([itemId])
}
